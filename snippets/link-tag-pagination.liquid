{% comment %}
  This snippet allows us to add the link tags with
  the previous and next page to the current one,
  so that google understands that they are published.

  Usage:
 {%  render 'link-tag-pagination'
  prev:  paginate.previous.url ,
  next:  paginate.next.url,
  anchor: anchor
%}
{% endcomment %}
<link id="rel-prev" rel="prev" href="{{ prev }}{{ anchor }}"/>
<link id="rel-next" rel="next" href="{{ next }}{{ anchor }}"/>
